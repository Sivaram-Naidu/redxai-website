import React from "react";

const pageStyles = {
  padding: "120px 2rem 2rem",
  minHeight: "80vh",
  textAlign: "center",
};

const SalePage = () => {
  return (
    <div style={pageStyles}>
      <h1>Sale</h1>
      <p>All sale items will be displayed here.</p>
    </div>
  );
};

export default SalePage;
